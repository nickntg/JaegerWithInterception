@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>

    @using (Html.BeginForm("DownloadValues", "Index"))
    {
        <button type="submit">Call the API</button>
    }
</div>
